import * as React from "react"
import * as styles from "./LabelNew.module.scss"

const LabelNew = () => {
  return (
    <div className={styles.labelNew_wrapper}>
      <div className={styles.labelNew}>NEW</div>
    </div>
  )
}
export default LabelNew
