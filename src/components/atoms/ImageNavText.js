// import { getMainProps } from "gatsby-plugin-image/dist/src/components/hooks"
import * as React from "react"

const ImageNavText = (src, alt) => {
  return <img src={src} alt={alt} style={width}/>
}

export default ImageNavText
